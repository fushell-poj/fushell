name: workspace_preview
description: Standalone Wayland workspace inspector for Fushell.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fushell:
    path: ../../packages/fushell

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
